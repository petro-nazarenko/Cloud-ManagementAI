'use strict';

const User = require('./User');
const Resource = require('./Resource');
const Recommendation = require('./Recommendation');
const AuditLog = require('./AuditLog');

module.exports = { User, Resource, Recommendation, AuditLog };
